{
  "name": "crash-game-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build"
  },
  "dependencies": {
    "axios": "^1.17.0",
    "socket.io-client": "^4.8.3"
  }
}
